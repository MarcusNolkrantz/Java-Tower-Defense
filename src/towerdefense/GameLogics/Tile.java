package towerdefense.gamelogics;

/**
 * Enum for the board's tiles. This enum's values are put in 2D arrays to resemble a board.
 */
public enum Tile
{
    /**
     * Tile enemies walk on.
     */
    PATH,
    /**
    * Tile towers can be placed on.
    */
    GROUND
}
